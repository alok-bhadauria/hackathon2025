<!DOCTYPE html>
<html>
<head>
    <title>Soil Classification</title>
    <link rel="stylesheet" href="/styles.css" />
</head>
<body>

<div class="container">
    <h1>Soil Classification System</h1>

    <form action="/predict" method="POST" enctype="multipart/form-data">
        <input type="file" name="image" required />
        <button type="submit">Analyze Soil</button>
    </form>

    <% if (error) { %>
        <div class="error"><%= error %></div>
    <% } %>

    <% if (result) { %>
        <div class="result-box">
            <h2>Prediction Result</h2>

            <p><strong>Soil Type:</strong> <%= result.soil_type %></p>
            <p><strong>Confidence:</strong> <%= (result.confidence * 100).toFixed(2) %>%</p>
            <p><strong>Description:</strong> <%= result.description %></p>
            <p><strong>Recommended Crops:</strong> <%= result.recommended_crops %></p>
            <p><strong>Best Season:</strong> <%= result.best_season %></p>
        </div>
    <% } %>
</div>

</body>
</html>
